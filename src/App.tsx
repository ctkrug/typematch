import "./styles/app-shell.css";

export function App() {
  return (
    <main className="app-shell">
      <h1 className="wordmark">
        <span className="wordmark-display">Type</span>
        <span className="wordmark-ui">match</span>
      </h1>
      <p className="tagline">
        Paste two Google Fonts. See the pairing across a real app UI, not a specimen sheet.
      </p>
    </main>
  );
}
